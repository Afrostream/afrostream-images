{
  "name": "afrostream-images",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "echo 'requiring node v4.2.6' && . ~/.nvm/nvm.sh && nvm use 4.2.6 && echo 'Now launching `node_modules/.bin/nodemon app/index.js`' && node_modules/.bin/nodemon app/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "4.8.4"
  },
  "private": true,
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express-cluster": "0.0.4",
    "http-proxy": "^1.13.0",
    "lodash": "^4.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.8.1"
  }
}
